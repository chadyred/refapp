{% extends 'melonRefappBundle::layout.html.twig' %}

	{% block menu_deroulant %}
		{{ parent() }}
	{% endblock %}
	
	{% block login %}
	{% endblock %}

	{% block contenu %}
		<div id='corps'>
			<h2>Ajouter une Technologie</h2>
				<div class="form">
  					<form method="post" {{ form_enctype(form) }}>
  						{# Les erreurs g�n�rales du formulaire. #}
						{{ form_errors(form) }}
						<div>  
							{# G�n�ration du label. #}
  							{{ form_label(form.nom, "Nom* :") }}
 
  							{# Affichage des erreurs pour ce champ pr�cis. #}
  							{{ form_errors(form.nom) }}
 
  							{# G�n�ration de l'input. #}
  							{{ form_widget(form.nom) }}
						</div>
						
						{{ form_rest(form) }}
						
						<h5>* : champ(s) obligatoire(s)</h5>
						
    				 	<input type="submit" class="btn btn-primary" value="Sauvegarder" id="boutonAjouterAdmin"/>
  					</form>
  					<a href="{{ path('_administration') }}">
						<input  type="submit" class="btn btn-primary" name="Annuler" value="Annuler"/>
					</a>
				</div>
		</div>
			
	{% endblock %}

